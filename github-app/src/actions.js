export const login = (f) => {
    return {
        type: 'LOGIN'
    }
};

export const logout = (f) => {
    return {
        type: 'LOGOUT'
    }
};

const loginAction = () => {
    return {
        type: 'LOGIN'
    }
};

const logoutAction = () => {
    return {
        type: 'LOGOUT'
    }
};